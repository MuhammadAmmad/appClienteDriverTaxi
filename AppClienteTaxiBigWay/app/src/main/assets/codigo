

                  /*  try {
                        Geocoder geocoder = new Geocoder(MainActivity.this, Locale.getDefault());
                        List<Address> list = geocoder.getFromLocation(Double.parseDouble(lat), Double.parseDouble(lng), 1);
                        if (!list.isEmpty()) {
                            Address address = list.get(0);
                            Toast.makeText(MainActivity.this,String.valueOf(address),Toast.LENGTH_SHORT).show();
                          //  messageTextView2.setText("Mi direcci—n es: \n" + address.getAddressLine(0));
                        }
                    } catch (IOException e) {
                        e.printStackTrace();
                    }*/